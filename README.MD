## SDK для получения данных о погоде с сайта openweathermap.org 

### Краткое описание  

  Разработанный SDK предоставляет интерфейс для API прогнозов погоды (https://openweathermap.org/api)

### 📋 Оглавление  

- [Установка](#-установка)
- [Использование](#-использование)
- [Функции](#-функции)

## 🚀 Установка
Предварительные требования: java 11 или выше.

1. Клонируйте репозиторий:
```bash
  git clone https://github.com/username/your-project.git
```
2. Создайте из исходников JAR-файл: Gradle→project_item→Tasks→build→jar(double click)
3. Скопировать полученный JAR-файл из структуры проекта project_name→build→libs→*.jar в директорию target вашего проекта, your_project/target (работает для Intellij Idea)
4. Подключите скопированный jar-файл в зависимостях: Project structure→Modules→Dependencies”+”→ указать путь к jar-файлу в директории “target” проекта.

## 💻 Использование
**Два кейса:**
1. Получение прогноза для списка городов из открытого API https://openweathermap.org/api
2. Получение получение прогноза по городам полученным в п 1. из хранилища Sdk
Пример:
```java
package com.attempt;

import org.weather.sdk.*;
import org.weather.sdk.exception.ApiKeyException;

public class Main {
    public static void main(String[] args) {

        String apiKey = "YOUR API KEY";
        String another_api_key = "another_api_key";
        WeatherSdkManager manager = WeatherSdkManager.getInstance();

        System.out.println("=== Mode SdkMode.ON_DEMAND ===");
        WeatherSdk sdk = JsonDoc.getInstance(apiKey, SdkMode.ON_DEMAND);

        System.out.println("===========Получаем прогноз погоды с помощью API============");
        long start = System.currentTimeMillis();
        System.out.println(JsonDoc.forecast(sdk, "London"));
        System.out.println(JsonDoc.forecast(sdk, "Moscow"));
        System.out.println(JsonDoc.forecast(sdk, "Washington"));
        System.out.println(JsonDoc.forecast(sdk, "Paris"));
        System.out.println(JsonDoc.forecast(sdk, "Belgorod"));
        System.out.println("Duration(openAPI): " + (System.currentTimeMillis() - start));

        System.out.println("===========Выводим прогноз погоды из кеша==============");
        start = System.currentTimeMillis();
        JsonDoc.forecastAllCities(sdk).forEach(System.out::println);
        System.out.println("Duration (cacheSDK): " + (System.currentTimeMillis() - start));

        // Создание Sdk в режиме опроса
        System.out.println("=== Mode SdkMode.ON_POLLING ===");
        try {
            WeatherSdk SdkPolling = manager.createSdk(another_api_key, SdkMode.POLLING);
        } catch (ApiKeyException e) {
            throw new RuntimeException(e);
        }

        System.out.println("=== Демонстрация, запрета создания второго экземпляра с одним ключом ===");
        try {
            manager.createSdk(apiKey, SdkMode.ON_DEMAND);
        } catch (Exception e) {
            System.out.println("Ожидаемая ошибка: " + e.getMessage());
        }
    }
}
```

## ✨ Функции
**Реализована работа в двух режимах:**  
- SdkMode.ON_DEMAND - режим «по запросу»  
- SdkMode.POLLING – режим опроса  

    **В режиме «по запросу»** SDK обновляет информацию о погоде только по запросам клиентов.  
  Данные о погоде хранятся во внутреннем хранилище, реализованном на MAP<>(), из которого пользователям отправляется информация, если она актуальна. Актуальной считается информация в течение последних 10 минут с момента ее сохраннеия в хранилище. Устаревшие данные обноляются вновь запросшенными с сайта данными и возвращаются клиенту.  
    **В режиме опроса** SDK запрашивает новую информацию о погоде для всех сохранённых в хранилище ранее городов каждые 10 минут.
  Для экономии памяти SDK может хранить информацию не более, чем для 10 городов одновременно. При добавлении информации по городу, который еще не был запрошен из последних 10, самые старые данные о погоде одного из городов замещаются данными о погоде о впервые запрошенном городе.